{
  "name": "studiopractice-server",
  "private": true,
  "type": "module",
  "description": "System of record for jobs: accounts, projects, and the drawing row each project owns.",
  "scripts": {
    "start": "node src/server.js",
    "migrate": "node db/migrate.js",
    "pretest": "node db/migrate.js",
    "test": "node --test \"tests/**/*.test.js\""
  },
  "dependencies": {
    "pg": "^8.13.1"
  }
}
